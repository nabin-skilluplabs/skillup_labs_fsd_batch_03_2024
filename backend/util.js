 function sum(a, b) {
   return (a+b);
}
//Create a function named sub which takes two values and substracts second values 
//and returns the results 
//Use that function in app.js and print the Result.
function sub(a, b) {
    return (a - b);
 }


module.exports = { sum , sub };
